apiVersion: v1
kind: Service
metadata:
  name: inventario-app-canary-service
  labels:
    app: inventario-app
spec:
  type: NodePort
  selector:
    app: inventario-app
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
